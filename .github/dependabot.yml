version: 2
updates:
  # Keep the SHA-pinned GitHub Actions up to date (Dependabot understands the
  # "uses: org/action@<sha> # vX" form and bumps both the SHA and the comment).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
